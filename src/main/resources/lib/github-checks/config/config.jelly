<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

  <f:entry title="${%Verbose console log}" field="verboseConsoleLog">
    <f:checkbox/>
  </f:entry>

  <f:entry title="${%Override Commit Environment Variable}" field="commitEnvVarOverride">
    <f:textbox default=""/>
  </f:entry>

  <f:entry title="${%Override Repository Environment Variable}" field="repoEnvVarOverride">
    <f:textbox default=""/>
  </f:entry>

</j:jelly>
